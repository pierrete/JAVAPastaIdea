public class MathAu7 {
    public static void main(String[] args) {
        //A7 - 20:28
        int numA = 5;
        float numB = 6f;

        /*

        //MATH.ABS Valor Absoluto (Se for negativo, exibe positivo)
        int resultado = Math.abs(numA);
        System.out.println(resultado);

        //MATH.POW Função de expoentes. Usa double como tipo base. Pode trocar o tipo ou usar casting para forçar sair inteiro.
        int resultado = (int)Math.pow(10,2); // 10 elevado à 2
        System.out.println(resultado);

        //MATH.SQRT  Raíz quadrada - só passa um número como todos vão ser multiplicado por 2.
        int resultado = (int) Math.sqrt(4);
        System.out.println(resultado);

        //MATH.CEIL  - Arredonda para mais o número (double)
        //Exceções negativas pode -0.2 se torna -0.0 e -4.36 se torna -4.0

        double resultado = Math.ceil(4.36);
        System.out.println(resultado);


        //MATH.FLOOR - Arredonda para menos
        double resultado = Math.floor(4.76);
        System.out.println(resultado);

        //MATH.MIN - retorna o menor entre os valores.
        double resultado = Math.min(numA,numB);
        System.out.println(resultado);

        //MATH.MAX - retorna o maior entre os valores.
        double resultado = Math.max(numA,numB);
        System.out.println(resultado);

         */



    }
}
